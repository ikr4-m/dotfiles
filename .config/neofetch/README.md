Minimal Neofetch configuration!

## How to use
1. Copy all this folder in `~/.config/neofetch`.
2. Call the configuration that you want with this command:
```
neofetch --config /path/to/config
```
3. Done!
